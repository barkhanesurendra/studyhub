CREATE DATABASE IF NOT EXISTS studyhub; USE studyhub;
-- The Node server creates all required tables automatically.
-- To make a user an admin after registration:
-- UPDATE users SET role='admin' WHERE email='your@email.com';